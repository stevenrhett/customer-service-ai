# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.8.0
pydantic-settings>=2.1.0

# AI/LLM Framework
langchain>=0.1.0,<1.0.0
langchain-openai>=0.0.2,<1.0.0
langchain-aws>=0.1.0,<1.0.0
langchain-community>=0.0.10,<1.0.0
langgraph>=0.0.20,<2.0.0

# Vector Database
chromadb==0.4.18

# Embeddings
sentence-transformers>=2.2.2,<3.0.0
langchain-huggingface>=0.0.1
huggingface-hub<0.20.0  # Pin to avoid compatibility issues with sentence-transformers
tf-keras  # Required for Keras 3 compatibility with transformers
numpy<2.0.0  # Required for compatibility with tensorflow/sentence-transformers
# Note: protobuf version managed by tensorflow/tf-keras
# tf-keras requires protobuf>=5.28.0, which may conflict with some packages
# This is generally safe to ignore unless you encounter runtime errors

# AWS
boto3>=1.34.72
botocore>=1.34.72

# OpenAI
openai>=1.0.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6

# Security & Rate Limiting
slowapi==0.1.9
html-sanitizer==2.2.0
